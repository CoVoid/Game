// gettin into that REALLY complicated stuff now
function Player(x, y, z) {
    this.x = x;
    this.y = y;
    this.z = z;

    function move(x, y, z) {
        this.x = x;
        this.y = y;
        this.z = z;
    }
}
